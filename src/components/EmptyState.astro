---
const {eyebrow,title,body,cta='Join the Ripple',href='/join'}=Astro.props;
---
<section class="empty"><p class="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{body}</p><a class="text-link" href={href}>{cta} <span>→</span></a></section>
